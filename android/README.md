# CrystalBeat (Android)

This is a minimal native Android (Kotlin) WebView wrapper for your CrystalBeat HTML game.

## Build (GitHub Actions)
Push to GitHub → Actions → Build APK → download artifact `app-debug.apk`.

## Local build (Android Studio)
Open the project, sync Gradle, run `app`.
